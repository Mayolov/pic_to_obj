// Shared constants used across modules.
// Adjust these to tune quality vs. performance.

/** Resolution of the depth-map canvas (pixels in each dimension). */
export const DEPTH_RES = 256;

/** How much the depth values displace mesh vertices in the Z axis. */
export const DEPTH_SCALE = 0.45;
